// Fixed enum matching API-Football's `position` field values - unlike
// nationality, this is a small closed set so a strict dropdown (rather than
// a free-text datalist) makes sense here.
export const POSITIONS = ["Goalkeeper", "Defender", "Midfielder", "Attacker"];
